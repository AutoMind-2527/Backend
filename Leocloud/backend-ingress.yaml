spec:
  ingressClassName: nginx
  rules:
  - host: if220129.cloud.htl-leonding.ac.at
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: backend-service
            port:
              number: 5191
      - path: /swagger
        pathType: Prefix
        backend:
          service:
            name: backend-service
            port:
              number: 5191
